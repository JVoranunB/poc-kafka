/* eslint-disable no-undef */
//const {  } = require('./app'); 

describe('unit Test', () => {
    test('app unit test', () => {
        expect(true).toBe(true);
        //expect(a('racecar')).toBe(true);
    });
});